name: Instagram Auto Post (Voice Reel)

on:
  schedule:
    # JSTの朝7:00 = UTC前日22:00
    - cron: "0 22 * * *"
    # JSTの夜21:00 = UTC同日12:00
    - cron: "0 12 * * *"
  workflow_dispatch:  # 手動実行用

jobs:
  post:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: pip install requests

      - name: Run post_reel_next.py --voice
        env:
          IG_USER_ID: ${{ secrets.IG_USER_ID }}
          IG_ACCESS_TOKEN: ${{ secrets.IG_ACCESS_TOKEN }}
        run: python3 post_reel_next.py --voice

      - name: Commit logs
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add reel_posted.log ig_embed_map.json
          git diff --staged --quiet || git commit -m "log: voice reel posted $(date +%Y-%m-%d_%H:%M)"
          git push
